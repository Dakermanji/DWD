<!-- views/home/main.ejs -->
<div class="background-overlay"></div>
<!-- Hero Section -->
<%- include('_hero') %>

<!-- About Section -->
<%- include('_about') %>

<!-- Services Section -->
<%- include('_services') %>

<!-- Portfolio Section -->
<%- include('_portfolio') %>

<!-- Contact Section -->
<%- include('_contact') %>

<!-- MODALS -->
<%
  const modal = (flash?.modal && flash.modal[0]) || null;
%>

<% if (!user) { %>
  <!-- Guest -->
  <% if (!modal || modal === 'login' || modal === 'register') { %>
    <%- include('../modals/auth/_authTabs', { modal }) %>
  <% } %>

  <% if (modal === 'complete_signup') { %>
    <!-- include('../modals/auth/_setUsernameWithPassword') -->
  <% } %>
<% } %>

<% if (user && !user.username) { %>
    <!-- OAuth completion -->
    <!-- include('../modals/auth/_setUsernameOnly') -->
<% } %>

<!-- Reset Password (future) -->
<!-- Conditional Reset Password Modal (future) -->
